export const SSE_ENDPOINT = '/overlay/listen';

export const BREAK_CHANCE = 0.01;
export const MIN_BREAK_DURATION = 1000; // Duration = 1000 * time in seconds
export const MAX_BREAK_DURATION = 3000; // Duration = 1000 * time in seconds

export const JUMP_VELOCITY = 500; // Velocity = sqrt(height * 1962)
